# In development this is not needed — Vite proxies /api to localhost:8000.
# In production set this to your deployed backend URL (no trailing slash).
VITE_API_BASE_URL=https://your-backend.onrender.com/api
